import ReactDOM from "react-dom";
import React from "react";
import App from "./App.js";

ReactDOM.render(
	<App />, // this just takes in the app that we are using
	document.getElementById('root')
);
